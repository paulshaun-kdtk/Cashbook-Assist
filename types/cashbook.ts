export interface Cashbook {
    name: string;
    which_key: string;
    currency: string;
    currency_symbol: string;
    description: string;
    $id: string;
    $sequence: string;
    $createdAt: string;
    $updatedAt: string;
    which_company: string;
}